# Brief: 마크다운 링크 이동

## Problem
"md full support" 축의 다음 항목으로 "링크 이동"(문서 내 앵커 점프, 외부 URL 열기, 다른 `.md`
파일로 이동 + back/forward 히스토리)을 검토했다. 조사 결과 이 기능은 dg에 **전혀 없다** —
링크는 그냥 `텍스트 (URL)`로 찍히는 정적 텍스트일 뿐이고, 페이저는 스크롤 전용이라 "지금
커서가 링크 위에 있다"는 개념 자체가 없다. 코드펜스 신택스 하이라이팅도 같이 논의됐으나
사용자가 이번 라운드에서는 보류하기로 확인했다(별도 스프린트에서 검토).

## Current State
- `markdown/mod.rs`의 `Tag::Link { dest_url, .. }` 처리는 `dest_url`을 링크 스타일로 감싼
  텍스트로만 남긴다(`self.link_url = Some(...)`는 있지만 각주와 달리 위치·목적지 매핑을 따로
  저장하지 않음) — 렌더링 후엔 "이 줄 이 칸이 어떤 URL로 가는 링크"인지 알 방법이 없다.
- 페이저(`pager.rs`)는 마우스 클릭 좌표 → 다이어그램 블록 토글(`toggle_block_at`)이라는 좌표
  기반 상호작용 선례가 이미 있다 — 링크 클릭도 같은 패턴(좌표 → 무엇을 할지 판단)을 재사용할
  수 있다. 다만 **키보드만으로**(마우스 없는 SSH 세션 등) 링크를 고르는 개념은 전혀 없다 —
  지금 키 배치(`q`/`j`/`k`/`Ctrl-d`/`Ctrl-u`/`Ctrl-f`/`Ctrl-b`/`Space`/`PageUp`/`g`/`G`/`/`/`n`/
  `N`/`o`/`r`)에 "포커스된 요소" 개념이 없다.
- `dg-watch-mode` 스펙이 최근 `Pager`에 `source: String` 필드 + `rerender()` 메서드를 이미
  추가해 뒀다 — "소스를 다른 내용으로 교체하고 다시 그린다"는 정확히 다른 파일로 이동할 때
  필요한 메커니즘과 같다. 처음부터 새로 만들 필요 없이 이 경로를 재사용할 수 있다.
- `main.rs`는 시작 시 파일 하나(또는 표준입력)를 한 번 읽는다. 표준입력으로 받은 문서는
  "현재 파일 경로"가 없어 상대 링크를 풀 기준 디렉터리가 없다 — 다른 파일로 이동은 이 경우
  자연히 불가능하다(감시 모드가 이미 표준입력을 배제한 것과 같은 종류의 제약).
- **경계 재검토**: 1차 discovery는 "파일브라우저"를 의존성/스코프 문제로 배제했었다. 이번에
  사용자가 확인한 "다른 `.md` 파일로 이동"은 디렉터리를 나열해 고르는 브라우징 UI가 아니라,
  **문서 저자가 이미 적어 둔 링크를 따라가는 것**뿐이다 — 배제 취소가 아니라 범위를 좁혀
  구분한다(자세한 근거는 Boundary Candidates).
- lazy/뷰포트 파싱으로 바꾸는 방안도 논의됐으나, 다이어그램은 이미 블록 단위로 독립 처리되고
  있고(코드펜스 하나를 만날 때 그 자리에서 바로 렌더링), 이번에 추가하는 앵커 점프는 전체를
  미리 파싱해 둔 현재 구조(`self.top = 줄 번호`, O(1))가 오히려 더 유리해 채택하지 않는다
  (검색의 "N개 중 M번째" 정확한 카운트도 전체 파싱 전제).

## Desired Outcome
- 문서 안 `[텍스트](#헤딩)` 링크를 따라가면 해당 헤딩으로 스크롤 이동한다.
- 문서 안 `[텍스트](http(s)://...)` 링크를 따라가면 시스템 기본 브라우저가 열린다(`xdg-open`/
  `open` 셸아웃, 신규 의존성 없음).
- 문서 안 `[텍스트](다른파일.md)` 링크를 따라가면 그 파일이 같은 페이저 화면에 열리고, 뒤로/
  앞으로 키로 이전/다음 문서·위치로 오간다.
- 마우스 클릭과 키보드 둘 다로 링크를 고를 수 있다(SSH 등 마우스 없는 환경 고려).
- 코드펜스 신택스 하이라이팅은 이번 스펙 범위 밖(별도 라운드).

## Approach
`Document`에 "이 줄의 이 칸 범위는 이런 종류(앵커/외부/파일)의 목적지를 가진 링크"라는 메타
데이터를 렌더링 시점에 함께 만든다(각주가 이미 위치·이름을 다루는 것과 비슷한 결의 확장).
페이저는 그 메타데이터로 (a) 마우스 클릭 좌표 판정 — 기존 `toggle_block_at` 패턴 재사용,
(b) 키보드로 링크 포커스를 옮기는 새 상호작용(다음/이전 링크로 포커스 이동 + 시각적 강조 +
확정 키로 이동)을 추가한다. 파일 이동은 `dg-watch-mode`가 만든 `source` 교체 +
`rerender()` 경로를 재사용하고, 그 위에 방문 기록(뒤로/앞으로) 스택을 얹는다. 외부 URL은
`std::process::Command`로 OS 기본 오프너를 셸아웃한다(플랫폼별 명령어 분기 필요:
`xdg-open`/`open`/`cmd /c start`).

## Scope
- **In**: 문서 내 앵커 점프, 외부 URL 브라우저 열기, 다른 `.md` 파일로 이동 + back/forward
  히스토리, 마우스·키보드 양쪽 링크 선택
- **Out**: 코드펜스 신택스 하이라이팅(보류, 별도 라운드), 디렉터리 브라우징/파일 선택 UI(여전히
  배제), 표준입력으로 받은 문서에서의 파일 간 이동(경로 기준 없음), lazy/뷰포트 파싱으로의
  구조 전환(불필요하다고 판단)

## Boundary Candidates
- 링크 종류 판별(앵커 `#`/외부 URL `http(s)://`/상대 파일 경로) 및 각각의 동작
- 키보드 링크 포커스 상호작용(다음/이전 링크 이동 키, 시각적 강조, 확정 키) — 정확한 키 배치는
  requirements에서 확정
- back/forward 히스토리의 범위(같은 파일 안 스크롤 위치 변화도 기록할지, 파일 전환만 기록할지)

## Out of Boundary
- 디렉터리 브라우징/파일 선택 UI — 1차 discovery의 배제 유지, 이번엔 "링크를 따라가는 것"만
- 코드펜스 신택스 하이라이팅 — 사용자가 이번 라운드에서 명시적으로 보류
- lazy/뷰포트 파싱 구조 전환 — 다이어그램은 이미 블록 단위, 점프·검색은 오히려 전체 파싱이
  유리해 바꿀 이유 없음

## Upstream / Downstream
- **Upstream**: `markdown/mod.rs`(`Renderer::handle`의 `Tag::Link`/헤딩 처리 — 앵커 슬러그
  생성), `pager.rs`(`dg-watch-mode`가 추가한 `source`/`rerender()`, `dg-watch-mode`의 감시
  대상 갱신과의 상호작용 — 파일 이동 시 감시 파일도 바뀌어야 하는지), `main.rs`(현재 파일
  경로를 페이저에 넘겨 상대 링크 해석 기준으로 씀)
- **Downstream**: README 페이저 키 표, robustness 테스트(존재하지 않는 파일 링크·순환 링크 등)

## Existing Spec Touchpoints
- **Extends**: `dg-watch-mode`(같은 `source` 교체·`rerender()` 경로를 파일 이동에도 재사용 —
  다만 이 스펙의 Boundary Commitment를 건드리는 게 아니라 같은 메커니즘을 다른 트리거로 또
  쓰는 것)
- **Adjacent**: `markdown-gfm-alerts`(같은 `markdown/mod.rs`, 관심사는 겹치지 않음)

## Constraints
- 신규 Cargo 의존성 없음 — 브라우저 열기는 셸아웃(`std::process::Command`), 파일 이동은
  `std::fs::read_to_string` 재사용
- panic 금지 계약 유지
- 표준입력 입력에는 파일 간 이동을 적용하지 않음(경로 기준 없음 — `dg-watch-mode`와 같은 제약)
